name: Coleta Dados Hidrometeorológicos RS

on:
  schedule:
    - cron: "*/10 * * * *"   # a cada 10 minutos
  workflow_dispatch:           # disparo manual via GitHub UI

env:
  PYTHON_VERSION: "3.11"
  DB_PATH: data/climate.duckdb
  TZ: America/Sao_Paulo

jobs:
  # ─────────────────────────────────────────────────────────
  # JOB 1 — Coleta ANA HidroWeb + INMET + Open-Meteo
  # ─────────────────────────────────────────────────────────
  collect:
    name: Coletar dados
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Checkout repositório de dados
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Checkout código-fonte (dashboard)
        uses: actions/checkout@v4
        with:
          repository: rafa-rebelo/climate-dashboard-rs
          token: ${{ secrets.GH_TOKEN }}
          path: _src

      - name: Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: _src/requirements.txt

      - name: Instalar dependências
        run: pip install -r _src/requirements.txt --quiet

      - name: Coletar ANA HidroWeb (níveis + chuva)
        env:
          ANA_IDENTIFICADOR: ${{ secrets.ANA_IDENTIFICADOR }}
          ANA_SENHA:         ${{ secrets.ANA_SENHA }}
          DB_PATH:           ${{ env.DB_PATH }}
        run: |
          cd _src
          python -m collectors.ana_collector
        continue-on-error: true

      - name: Coletar previsões Open-Meteo / NOAA
        env:
          DB_PATH: ${{ env.DB_PATH }}
        run: |
          cd _src
          python -m collectors.noaa_collector
        continue-on-error: true

      - name: Calcular acumulados de chuva
        env:
          DB_PATH: ${{ env.DB_PATH }}
        run: |
          cd _src
          python -m processors.rain_accumulator

      - name: Gerar relatório de progresso
        env:
          DB_PATH: ${{ env.DB_PATH }}
        run: |
          cd _src
          PYTHONPATH=src python -m utils.progress_tracker

      - name: Copiar artefatos atualizados → repositório de dados
        run: |
          cp _src/data/climate.duckdb                    data/climate.duckdb
          cp _src/data/processed/accumulated_rain.parquet data/processed/
          cp _src/data/processed/river_status.parquet     data/processed/
          cp _src/data/processed/progress.json            data/processed/
          cp _src/data/forecasts/nwp_7days.parquet        data/forecasts/ 2>/dev/null || true
          cp _src/data/forecasts/nwp_7days_daily.parquet  data/forecasts/ 2>/dev/null || true
          cp _src/JSONCRACK_FLUXOGRAMA.json               . 2>/dev/null || true

      - name: Commit e push dos dados atualizados
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ JSONCRACK_FLUXOGRAMA.json 2>/dev/null || git add data/
          # Só commita se houver mudança real
          git diff --cached --quiet && echo "Sem alterações — skip commit" && exit 0
          TIMESTAMP=$(TZ=America/Sao_Paulo date "+%Y-%m-%d %H:%M BRT")
          git commit -m "data: atualização automática ${TIMESTAMP}"
          git push

  # ─────────────────────────────────────────────────────────
  # JOB 2 — Verificação de alertas (depende do job collect)
  # ─────────────────────────────────────────────────────────
  alerts:
    name: Verificar alertas Telegram
    runs-on: ubuntu-latest
    needs: collect
    timeout-minutes: 3
    if: success()

    steps:
      - name: Checkout código-fonte
        uses: actions/checkout@v4
        with:
          repository: rafa-rebelo/climate-dashboard-rs
          token: ${{ secrets.GH_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Instalar dependências
        run: pip install -r requirements.txt --quiet

      - name: Verificar alertas e notificar Telegram
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DB_PATH:          ${{ env.DB_PATH }}
        run: |
          PYTHONPATH=src python -c "
          from alerts.telegram_bot import run_alert_cycle
          run_alert_cycle()
          "
        continue-on-error: true
